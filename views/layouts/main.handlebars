<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CCS Airlines</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    .seat {
      width: 50px;
      height: 40px;
      line-height: 40px;
      text-align: center;
      border: 1px solid #ccc;
      border-radius: 5px;
      cursor: pointer;
      user-select: none;
    }
    .seat:hover {
      background-color: #e9ecef;
    }
    .seat.selected {
      background-color: #198754;
      color: white;
      border-color: #198754;
    }
    .spacing {
      width: 40px;
      display: inline-block;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">CCS Airlines</a>
      <div class="ms-auto">
        {{#if user}}
          {{#if (eq user.role "admin")}}
            <a class="btn btn-outline-light" href="/admin/flights">Manage Flights</a>
            <a class="btn btn-outline-light" href="/admin/users">Manage Users</a>
          {{else}}
            <a class="btn btn-outline-light" href="/flights/search">Search Flights</a>
            <a class="btn btn-outline-light" href="/reservations/list">My Reservations</a>
            <a class="btn btn-outline-light" href="/profile">Profile</a>
          {{/if}}
          <a class="btn btn-outline-danger" href="/logout">Logout</a>
        {{else}}
          <a class="btn btn-outline-light" href="/login">Login</a>
          <a class="btn btn-outline-light" href="/register">Register</a>
        {{/if}}
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    {{{body}}}
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
